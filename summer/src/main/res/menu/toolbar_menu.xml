<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".location.LocMap">

    <item
        android:id="@+id/action_aLine"
        android:icon="@mipmap/ic_idea"
        android:orderInCategory="70"
        android:title="All_Line"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_MyLine"
        android:icon="@mipmap/ic_abt"
        android:orderInCategory="80"
        android:title="My_Line"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_setoute"
        android:title="规划路线"
        android:orderInCategory="90"
        app:showAsAction="ifRoom"
     />

    <item
        android:id="@+id/action_out"
        android:orderInCategory="100"
        android:title="退出应用"
        app:showAsAction="ifRoom"/>

</menu>